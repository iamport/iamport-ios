    import XCTest
    @testable import iamport_ios

    final class iamport_iosTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(iamport_ios().text, "Hello, World!")
        }
    }
